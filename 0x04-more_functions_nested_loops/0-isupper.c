#include "main.h"
/**
 * _isupper- checks for uppercase character
 *
 * @c: value to be checked
 *
 * Return: 1 if c is uppercase, otherwise 0
 */
int _isupper(int c)
{
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
